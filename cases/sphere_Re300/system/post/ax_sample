ax_sample
{
    type sets;
    libs                  (sampling);
    interpolationScheme   cellPointFace;
    setFormat             csv;
    fields                (U UMean UPrime UPrime2Mean p pMean);

    sets
    (
        axis
        {
          type    uniform;
          nPoints 1000;
          axis    x;
          start   (0 0 0);
          end     (25 0 0);
        }
        
    );
    
    ${/averageControl}; 
    ${/execControl};
}


